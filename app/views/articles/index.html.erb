<h1>Articles</h1>
Our blog has <%= Article.public_count %> articles and counting!
<ul>
  <% @articles.each do |article|%>
    <% unless article.archived? %>
    <%# unless:でない時 statusがarchivedでない時 %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "NEW Articel", new_article_path %>